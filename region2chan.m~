%This function record channels number corresponding to a given region in
%the DK atlas
function C=region2chan(Lr,ROI)

for i=1:size(ROI)
    S=ROI(i);
    idx= strfind(Lr,S);
    C(i) = find(not(cellfun('isempty', idx)));
    end
end